dist/
.venv/
__pycache__/
*.pyc
.DS_Store
# Node tooling for browser smoke tests. The manifests (package.json,
# package-lock.json) and harnesses (tests/browser/) are committed; only the
# installed packages are not — run `npm install` to get puppeteer.
node_modules/

# Generated chapter screenshots (regenerate via tests/browser/shot.mjs)
cartpole-*.png

# Workspace meta dirs (not part of the project)
.xdg-config/
.claude-memory/
